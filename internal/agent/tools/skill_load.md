Load the complete instructions for one active skill by name and mark it loaded for the current agent session.

Use this application service instead of reading SKILL.md through the generic view tool. Load a matching skill before performing the task it governs.
