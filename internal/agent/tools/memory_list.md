List persistent memories in one scope, or read one topic in full.

Use `project` for decisions, context, and references specific to the current project. Use `user` for preferences, feedback, responsibilities, and references that apply across projects. Always inspect the target scope before an upsert or removal so related memories are updated instead of duplicated and memories that are no longer relevant can be removed.
