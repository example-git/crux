Use this only while executing an approved plan, after every plan item is implemented and the relevant validation has completed. Present the implementation and validation summary in the assistant response, then pass the same concise summary in `summary` for user review.

User approval confirms that the work itself is complete, clears the persisted plan, and restores normal instructions. A request to continue working keeps the approved plan active and returns the user's remaining-work feedback.
