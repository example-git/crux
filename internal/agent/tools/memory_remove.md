Remove one persistent memory from the selected project or user scope.

Before calling this tool, use `memory_list` to inspect the target scope and confirm the topic is no longer relevant, stale, duplicated, superseded, or explicitly requested to be forgotten. Do not remove useful memory solely to meet a target count. The managed memory index is rebuilt automatically.
